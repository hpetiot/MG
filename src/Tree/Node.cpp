#include "Node.hpp"

bool Node::addSon(Node son){
    std::cerr << "ERROR: this implmentation of autobox should never be reached" << std::endl;
    exit(EXIT_FAILURE);
}

int Node::getWeight(){
    std::cerr << "ERROR: this implmentation of autobox should never be reached" << std::endl;
    exit(EXIT_FAILURE);
}

double Node::Value(const Vector& candidate) const {
    std::cerr << "ERROR: this implmentation of autobox should never be reached" << std::endl;
    exit(EXIT_FAILURE);
}

Vector Node::Gradient(const Vector& point) const {
    std::cerr << "ERROR: this implmentation of autobox should never be reached" << std::endl;
    exit(EXIT_FAILURE);
}

const Box Node::autoBox() const{
    std::cerr << "ERROR: this implmentation of autobox should never be reached" << std::endl;
    exit(EXIT_FAILURE);
}